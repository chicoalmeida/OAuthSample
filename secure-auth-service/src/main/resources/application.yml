server:
  port: 9000
  context-path: /services
security:
  user:
    role: USER
  oauth2:
    client:
      client-id: javabeer
      client-secret: javabeer
      authorized-grant-types: authorization_code,refresh_token,password,client_credentials
      scope: toll_read,toll_report